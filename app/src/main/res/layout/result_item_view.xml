<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/result_item_container"
    android:layout_width="match_parent"
    android:layout_height="@dimen/result_item_height"
    android:paddingLeft="@dimen/result_item_horizontal_margin"
    android:paddingRight="@dimen/result_item_horizontal_margin"
    android:paddingTop="@dimen/result_item_vertical_margin"
    android:clickable="true"
    android:orientation="vertical"
    android:background="?attr/selectableItemBackground">

    <TextView
        android:id="@+id/result_item_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:maxLines="1"
        android:drawableLeft="@drawable/ic_repo"
        android:textSize="@dimen/result_item_title_size"
        android:gravity="center_vertical"/>

    <TextView
        android:id="@+id/result_item_id"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:maxLines="1"
        android:textSize="@dimen/result_item_id_size"/>

</LinearLayout>